locks:
    lock: Lock
    locks: Locks
    lock_type: 'Lock Type'
    my_locked_items: 'My locked items'
    no_locked_items: 'No locked items'
    remove_lock: 'Remove lock'
    remove_locks: 'Remove locks'
    remove_my_locks: 'Remove my locks'
    remove_all_locks: 'Remove all locks'
    object_type: 'Object type'
    object_id: 'Object ID'
    date: Date
